<body>

   <div class="container">
      <div class="card o-hidden border-5 shadow-lg my-5">
         <div class="card-body p-0">
            <!-- Nested Row within Card Body -->
            <div class="row">
               <div class="col-lg">
                  <div class="p-5">
                     <div class="text-center">
                        <h1 class="h4 text-gray-900 mb-4">EDIT CATEGORY</h1>
                     </div>
                     <form class="user">
                        <div class="form-group row">
                           <input type="text" class="form-control form-control-user text-center" id="cat"
                              placeholder="Category" name="category" value="{{category.category}}">
                        </div>
                        {{!-- <input type="submit"> --}}

                     </form>
                     <a onclick="editcategory('{{category._id}}')" class="btn btn-user btn-block"
                        style="background-color: #FB641B;color:white ;"> EDIT NOW
                     </a>
                  </div>
               </div>
            </div>
         </div>
      </div>
   </div>

   <script>
      function editcategory(catId) {
         var category = document.getElementById('cat').value
         $.ajax({
            url: `/admin/editcategory/${catId}`,
            method: "post",
            data: {
               category: category
            },
            success: (response) => {
               if (response.status) {
                  Swal.fire(
                     'Updation Done',
                     'You updated the details of categories',
                     'success'
                  ).then((state) => {
                     if (state) {
                        location.href = "/admin/viewcategories"
                     }
                  })
               }
            }
         })
      }
   </script>